To run a local server yourself in VS Code:

Open the integrated terminal (Ctrl + `).
Navigate to your project directory:

-- cd 'c:\Users\veron\INIT\init-tron-event'
-- Start the server:
--python -m http.server
-- Open your browser and go to:
http://localhost:8000/mainPage.html



More info:
echo "# init-tron-event" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/veronica-lab/init-tron-event.git
git push -u origin main